name = "ouoankang/moonformdata"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/ouoankang/moonformdata"

license = "Apache-2.0"

keywords = [ "multipart", "form-data", "upload", "schema", "validation" ]

preferred_target = "wasm-gc"

description = "Multipart upload contracts, schema validation, diagnostics, parsing, and encoding for MoonBit."
